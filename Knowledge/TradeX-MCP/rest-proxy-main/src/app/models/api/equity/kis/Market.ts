/**
 * @swagger
 * components:
 *   schemas:
 *     EstimateCeilingFloorResponse:
 *       type: object
 *       properties:
 *         ceilingPrice:
 *           type: number
 *           format: double
 *           description: the estimated ceiling price
 *         floorPrice:
 *           type: number
 *           format: double
 *           description: the estimated floor price
 */
