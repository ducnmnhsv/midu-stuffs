/**
 * @swagger
 * components:
 *   securitySchemes:
 *     secToken:
 *       type: apiKey
 *       in: header
 *       name: secToken
 *     secDomain:
 *       type: apiKey
 *       in: header
 *       name: secDomain
 * tags:
 *   - name: Authentication
 *     description: Authentication to login MTS System
 *   - name: User
 *     description: Operation for TradeX User
 *   - name: Favorite
 *     description: Operation for Favorite Stock List
 *   - name: Alarm
 *     description: Operation for Alarm Setting
 *   - name: News
 *     description: Query for News
 */

// Add common into the route

//Add api into the route
